# gms flavor — Firebase config

Copy `google-services.example.json` to `google-services.json` **in this
folder** and replace it with the real file from the
[Firebase console](https://console.firebase.google.com) (Android app with
package name `com.devquest.app`).

The real `google-services.json` is gitignored — never commit it.
